Config = {}

Config.Notifications = {
    BoomboxInventoryPickup = '^2Boombox stored in inventory.',
    BoomboxHeld = '^2You are now holding the boombox.',
    BoomboxPlaced = '^2Boombox placed on the ground.',
    BoomboxSpawned = '^2Boombox spawned.'
}

Config.TextHelpDropBoombox = "Press ~INPUT_CONTEXT~ to drop the boombox on the ground"


Config.Sound = {
    DefaultDistance = 20.0,   
    MaxDistance = 50.0        
}

